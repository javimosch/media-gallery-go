linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - misspell
    - gosimple

run:
  timeout: 2m

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
